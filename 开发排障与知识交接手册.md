# 扫地机器人项目 — 开发排障与知识交接手册

> 用途：供下一任 AI / 工程师直接读取使用，接续本项目。含环境信息、标准操作流程、全部踩坑记录与解法、当前进度与下一步。
> 版本：v1.1　日期：2026-08-29（新增：Nav2 排障全记录、SLAM 定位替代 AMCL、run_slam.sh 一键建图、当前 map→odom 排查）

---

## 0. 项目背景与最终决策（一句话版）

**目标**：造一台对标米家 M40 主机的商用扫地机器人（可量产、预留拖地）。

**最终架构决策**：
- 操作系统：**Ubuntu 22.04 + ROS2 Humble**（STM32 工具链限定 22.04；Humble 生态完整）
- 主控：**RK3566（Linux 算法侧）+ STM32G473（FreeRTOS 实时侧）** 双主控异构
- 安全：**硬安全下沉 MCU**（悬崖/碰撞/电流/CPU 看门狗，不经 ROS）
- 通信：MCU↔Linux 自定义隔离串口帧协议（量产）；micro-ROS 仅开发期/教育版
- 语言：**机器人端 100% C++（rclcpp）**，出货固件零 Python；launch 用 XML/YAML
- 开发路径：仿真先行 → 开源算法拼装 → 量产冻结用 Yocto 压到 2GB
- 工期：功能样机 20± 周；成本 743 元/零售 1699 元；B2B 先行 B2C 扩展
- 详见同目录《扫地机器人最终方案.md》（已含商用落地、许可合规、认证计划）

---

## 1. 环境信息（重要！）

| 项 | 值 |
|---|---|
| 开发机 | Windows（用户 Administrator），D 盘，VMware 虚拟机跑 Linux |
| 虚拟机 | Ubuntu **22.04.5**，ROS2 **Humble**，Gazebo **11.10.2**，内存 7.7G（可加到 16G） |
| 共享文件夹 | Windows: `C:\Users\Administrator\Desktop\机器人-项目结构\扫地机器人\robot-share` ↔ VM: `/mnt/hgfs/robot-share` |
| 工程位置 | 我的工作区 `D:\harness\vacuum_ws`（我改文件的地方）→ 共享文件夹 → VM `~/ros_ws` |
| VM 工作区 | `~/ros_ws`（src/ 下 vacuum_description、vacuum_demo、m-explore-ros2） |
| 保存的地图 | `~/ros_ws/maps/house.pgm` + `house.yaml`（两室公寓） |
| apt 源 | 阿里云（ubuntu + ros2）；ROS2 密钥：`apt-key adv --keyserver keyserver.ubuntu.com --recv-keys F42ED6FBAB17C654` |
| 参考硬件 | DC-A566B 开发板 = **RK3566**（Android 11/Linux，规格书无烧录章节，烧录用 RKDevTool + update.img，Loader 进入方法需问厂家定昌电子） |

**三跳同步流**（我改文件后的标准动作）：
```
D:\harness\vacuum_ws  →（我 Copy-Item，需 danger-full-access）→ robot-share\vacuum_ws
→（VM 里）bash /mnt/hgfs/robot-share/sync_vacuum.sh   ← 自动拷贝+colcon build
```
> 同步脚本：`robot-share/sync_vacuum.sh`（VM 用）、`robot-share/sync_to_share.ps1`（Windows 推文件用）。
> 若 VM 里共享文件夹丢了：`sudo vmhgfs-fuse .host:/ /mnt/hgfs -o allow_other`（**不要加 -o nonempty**，新版 fuse 不认）。

---

## 2. 标准操作流程（照做必通）

### 2.1 启动仿真（手动脚本，勿用 launch XML）

```bash
bash /mnt/hgfs/robot-share/sync_vacuum.sh   # 同步+构建（改了文件才需要）
pkill -9 -f gzserver; pkill -9 -f gzclient; sleep 1   # 清残留，防端口占用
bash ~/ros_ws/src/vacuum_description/launch/run_sim.sh
# GUI=1 bash ...  = 开 Gazebo 3D 窗口（虚拟机里会吃 CPU，默认无头）
```

### 2.2 建图（SLAM）——一键脚本

```bash
bash ~/ros_ws/src/vacuum_description/launch/run_slam.sh
# 自动：杀旧 slam → 等 /scan、/odom 出现 → 启动 async_slam_toolbox_node（显式参数，日志 /tmp/slam.log）→ 自检 map→odom TF
# 成功标志: 输出 SLAM PID: ...；日志出现 Registering sensor: [Custom Described Lidar]
```

手动等价命令（后台+日志，供排查用）：

```bash
source ~/ros_ws/install/setup.bash
nohup ros2 run slam_toolbox async_slam_toolbox_node --ros-args \
  -p use_sim_time:=true -p map_frame_name:=map -p odom_frame:=odom \
  -p base_frame:=base_footprint -p scan_topic:=/scan > /tmp/slam.log 2>&1 &
```

### 2.3 遥控（Humble 按键是 i/j/k/l，不是 w/s/a/d！）

```bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
# z=降速 i=前进 k=后退 j/l=左右转 空格=停
```

### 2.4 保存地图（必须仿真+SLAM 都在跑）

```bash
timeout 4 ros2 topic hz /map --window 3    # 先确认有频率
mkdir -p ~/ros_ws/maps
ros2 run nav2_map_server map_saver_cli -f ~/ros_ws/maps/house
ls -l ~/ros_ws/maps/
```

### 2.5 看地图

```bash
ros2 run rviz2 rviz2 --ros-args -p use_sim_time:=true   # Fixed Frame=map, Add→By topic→/map
```

### 2.6 启动 Nav2（导航）——只开 navigation_launch.py，**禁用 AMCL**

```bash
source ~/ros_ws/install/setup.bash
nohup ros2 launch nav2_bringup navigation_launch.py \
  params_file:=$HOME/ros_ws/src/vacuum_description/config/nav2_params.yaml \
  use_sim_time:=true > /tmp/nav2_nav.log 2>&1 &
# 绝不用 bringup_launch.py：它会带 AMCL（必崩，见 §3.11）和 map_server
```

### 2.7 导航冒烟测试

```bash
bash ~/ros_ws/src/vacuum_description/launch/run_nav_test.sh
# 1) 等 bt_navigator  2) 逐个 configure+activate 生命周期节点（timeout 5 防卡死）
# 3) 查 map→odom TF  4) 发送 /navigate_to_pose 到 (2.5, 2.8) 床，期望 SUCCEEDED
```

### 2.8 完整启动序列（会话中断/仿真死过后必须全套重来）

```bash
pkill -9 -f gzserver; pkill -9 -f gzclient; pkill -9 -f slam_toolbox
pkill -9 -f nav2_bringup; pkill -9 -f component_container; sleep 2
bash ~/ros_ws/src/vacuum_description/launch/run_sim.sh      # 等 ROBOT IS IN GAZEBO
bash ~/ros_ws/src/vacuum_description/launch/run_slam.sh     # 等 SLAM PID
# 然后按 §2.6 起 navigation_launch.py（nohup 后台）
bash ~/ros_ws/src/vacuum_description/launch/run_nav_test.sh
```

---

## 3. 排障记录大全（按主题，每坑=现象→原因→解法）

### 3.1 WSL2 安装（已放弃，改走虚拟机）
- `wsl --list --online` / `wsl --install` 报 **502**：WSL 在线功能依赖被墙的 `raw.githubusercontent.com`（发行版列表）+ 微软服务器。
- 尝试：TUNA/阿里/USTC 镜像 rootfs 全部 404（WSL 发行版是商店 Appx 渠道，不常驻 cloud-images）；`wslubuntu2404` 短链不可靠。
- **结论**：国内网络下放弃 WSL，改用 **VMware 虚拟机 + Ubuntu ISO**（ISO 走清华 `mirrors.tuna.tsinghua.edu.cn/ubuntu-releases/24.04/` 或 22.04）。
- 教训：**别信 AI 给的镜像文件 URL**（TUNA 404 页专门提示"LLM 链接很可能不存在"）；先 curl HEAD 验证再下。

### 3.2 Ubuntu/ROS2 安装（GPG 密钥坑）
- 现象：`apt update` 报 `NO_PUBKEY F42ED6FBAB17C654`，`ros-humble-desktop` 装不上。
- 原因：镜像的 `ros2.key` 路径全 404（TUNA/USTC/阿里都不在 `/ros2/ubuntu/ros2.key`），下载到的是 HTML 页；`signed-by=` 指向坏文件。
- 解法：`sudo apt-key adv --keyserver keyserver.ubuntu.com --recv-keys F42ED6FBAB17C654`（公钥服务器导入，**成功标志：已导入 1**），并把源里 `signed-by=` 删掉（用系统信任环）：
  `echo "deb [arch=amd64] http://mirrors.aliyun.com/ros2/ubuntu jammy main" > /etc/apt/sources.list.d/ros2.list`
- 教训：ROS2 装好后**必须验证应用层**：`ros2 pkg list | grep -cE "nav2|slam_toolbox|gazebo_ros"` 应 ≥20；Gazebo/Nav2 不在 desktop 里，要单独装 `ros-humble-gazebo-ros-pkgs ros-humble-slam-toolbox ros-humble-nav2-bringup ros-humble-teleop-twist-keyboard`。

### 3.3 launch XML 前端（Humble 2026 版是残的）——核心结论：别用
- `textfile` 属性不支持（Unexpected attribute）
- `name`+`from` 判为互斥（RuntimeError）
- 注释里中文/em-dash（U+2014）报 `invalid character '—'`
- `bash -c '...'` 嵌套引号报 `No closing quotation`；`&gt;` 实体也出错
- 多行字符串 param 值会被当 YAML 解析（`REP-103: x` 的冒号触发）
- **结论**：全程用 `run_sim.sh` 手动脚本（gazebo 系统二进制 + spawn + rsp），不再碰 XML launch。YAML launch 前端更健壮，可作后续备选。

### 3.4 URDF 模型（三个经典坑，全踩了）
- **坑1 轮子消失**：`diff_drive: Joint [left_wheel_joint] not found` + `gz model -i` 显示模型只有 1 个 link、0 个 joint。原因：**轮子 link 没写 `<inertial>`，URDF→SDF 转换把无质量子树丢弃**。解法：给 left/right/caster 轮补 `<inertial>`（TurtleBot3 等成熟模型都有）。
- **坑2 轮子翻跟头**：`rpy="0 1.5708 0"`（绕 Y 转）把圆柱轴转到 X，与关节轴 Y 垂直 → 不滚只翻。解法：`rpy="1.5707963267948966 0 0"`（绕 X 转，圆柱轴落到 Y=关节轴）。
- **坑3 关节名**：先试无前缀名、再试 `vacuum::` 前缀都不行——因为根因是坑1（关节根本不存在）。惯性补上后用无前缀名即可。
- 教训：**gazebo_ros 插件报 Joint not found 时，先 `gz model -m <name> -i` 看模型里到底有没有 joint**。

### 3.5 Gazebo 插件与话题
- **gzserver 没有 /spawn_entity**：裸 `gazebo`/`gzserver` 不加载 ROS 插件。必须：`gzserver -s libgazebo_ros_init.so -s libgazebo_ros_factory.so <world>`（`run_sim.sh` 已写好）。
- **雷达话题是 /lidar_scan/out 不是 /scan**：新版 gazebo_ros(3.9) ray sensor 默认 `~/out`。解法：插件里 `<remapping>~/out:=scan</remapping>`。
- **SDF 语法**：`<cylinder radius=.../>`、`<sphere radius=.../>` 是 URDF 写法，SDF 必须子元素 `<radius>`；**`<cone>` 不被 Gazebo 11（SDF 1.7）支持**，报 `Unknown geometry type` → 世界整体加载失败，必须用 box/cylinder/sphere。

### 3.6 性能与残留进程
- **gzclient 269% CPU**（虚拟机无 GPU 软渲染）：建图假死、teleop 失灵。解法：`run_sim.sh` 默认无头（`GUI=${GUI:-0}`），画面用 RViz；要 3D 窗口先开 VMware 3D 加速（VM→Settings→Display→Accelerate 3D graphics，**重启虚拟机生效**）再 `GUI=1`。
- **`Address already in use`（端口 11345）**：残留 gzserver。解法：`pkill -9 -f gzserver; pkill -9 -f gzclient`（-9 必须，SIGTERM 杀不干净）。
- **SLAM `Message Filter dropping message ... queue full`**：无害，虚拟机性能下 TF 查询偶尔掉帧，忽略。

### 3.7 保存地图
- `map_saver_cli` 报 `Failed to spin map subscription`：**仿真或 SLAM 没在跑，/map 不存在**。先 `hz /map` 确认再保存。
- `joint_cmd` 广告报错（wall_div_west）：静态墙无关节，无害，忽略。

### 3.8 其他
- teleop 按键布局：Humble 是 **i/j/k/l**（u/i/o/j/k/l/m/,/. 八方向），`w` 是调线性速度（最初按 w 只涨 speed 不动，就是这个）。
- 机器人"走反方向/自己转圈"：多半是 **odom_spinner 在抢 /cmd_vel**，`pkill -9 -f odom_spinner`。
- 雷达全 inf：空世界没有障碍物是正常的；要有墙/家具。
- Gazebo 视角：Ctrl+2 俯瞰、Follow 选模型名、鼠标中键旋转。

### 3.9 PDF 读取（中文规格书）
- 纯 PowerShell 解 FlateDecode 只能拿到 CID 字形 ID（乱码），CMap 手写解码失败。
- **可靠路线**：PDF 拷进共享文件夹 → VM 装 poppler-utils → `pdftoppm -png -r 120 xxx.pdf /mnt/hgfs/robot-share/pdftmp/page` → 用 modlens 逐页读（长页先裁剪成条再读）。
- 结论：DC-A566B 规格书 = RK3566 硬件规格，**无烧录章节**；烧录走 RKDevTool + update.img。

### 3.10 modlens 图片识别（本会话的坑）
- 报 `max_tokens参数非法`：modlens 配置坏了。修复：宿主 Windows 执行
  `npx @liustack/modlens config set openai.extraBody '{"max_tokens":1024}'`
  （范围必须 [1,1024]）再 `npx @liustack/modlens doctor`。
- 用户截图落盘在 `C:\Users\Administrator\AppData\Local\Temp\ScreenShot_*.png`，modlens 挂了可找最新文件换路读。

### 3.11 Nav2 全家桶（Humble）——本会话最大战场
- **AMCL 必崩**：`component_container_isolated` 容器"意外停止"，日志
  `pthread_mutex_lock.c:94: ___pthread_mutex_lock: Assertion 'mutex->__data.__owner == 0' failed`（`initTransforms` 内）。
  这是 nav2 已知 bug。**对策：永久绕过 AMCL**——定位用 slam_toolbox（async 模式同时发布 map→odom），
  只起 `navigation_launch.py`，**绝不 bringup_launch.py**（它带 AMCL+map_server）。
- **生命周期**：nav2 节点必须手动 `configure`+`activate`；`autostart:=true` 常中途放弃
  （map_server 缺 yaml_filename、planner/bt 插件库找不到）。`run_nav_test.sh` 从实时 node list 抓节点，
  逐个 `timeout 5 ros2 lifecycle set <n> configure/activate`（timeout 必须有，否则偶发卡死）。
- **插件名是斜杠不是双冒号**：planner `nav2_navfn_planner/NavfnPlanner`（`NavfnPlanner::` 找不到）；
  behavior 用 `nav2_behaviors/Spin` 等斜杠形式。
- **bt_navigator/behavior_server 的 plugin_lib_names**：必须与官方一致（46 个库），
  抄 `/opt/ros/humble/share/nav2_bringup/params/nav2_params.yaml`；
  **Humble 没有 `nav2_goal_updated_bt_node`**，是 `nav2_goal_updated_condition_bt_node`。
- **map_server 的 yaml_filename**：不写进 params 就报 `yaml_filename not initialized`
  （nav2_params.yaml 已写死 `/home/robot/ros_ws/maps/house.yaml`）。
- **Goal rejected**：多为 AMCL 死掉 / map frame 缺失；绕过 AMCL + slam TF 在即恢复（goal 重新被接受）。
- **Goal ABORTED**：`Control loop missed its desired rate of 20Hz` + `Failed to make progress`（VM CPU 饥饿）。
  对策：RPP 控制器 / velocity_smoother 降到 **10Hz**，progress_checker 放宽
  `required_movement_radius: 0.1`、`movement_time_allowance: 20`（已写进 nav2_params.yaml，待验证）。

### 3.12 SLAM 定位 map→odom 缺失（v1.1 时正在排查）
- 现象：Nav2 起得来、goal 能接受，但 TF 里没有 map→odom → 导航无法把目标从 map 转到 odom。
- 排查协议（按序）：
  `tail -n 200 /tmp/slam.log` → `ros2 node list | grep -i slam` → `ros2 topic hz /map --window 3`
  → `ros2 topic echo /tf --once` → `ros2 param get /async_slam_toolbox_node {map_frame_name,odom_frame,base_frame}`。
- 判断：节点没了 → slam 死/没起，直接 `run_slam.sh`；/map 无频率 → slam 没收到 scan/odom（看日志）；
  /map 有频率但无 TF → slam 的 TF 发布问题（看日志 error/warn，如 "Failed to publish tf" / "No transform found"）。
- **关键认知：async slam 收不到 /scan 就不发布 map→odom**——sim 中途死过而 slam 没跟着重启是最常见原因。
  会话间隔后所有仿真状态都会丢，必须按 §2.8 全套重启。
- 临时旁路（仅测试，别长期）：`ros2 run tf2_ros static_transform_publisher 0 0 0 0 0 0 0 map odom`
  发恒等 map→odom，可单独验证 Nav2 规划/控制链路；测完必 kill（Ctrl-C）。

### 3.13 帧一致性（已确认，无需改 URDF）
- 完整 TF 链：`map → odom（slam_toolbox）→ base_footprint（diff_drive 插件 robot_base_frame）
  → base_link → base_scan（robot_state_publisher）`；激光 frame = base_scan，
  与 diff_drive、URDF 一致 → **URDF 不用改**。
- 验证命令：`ros2 run tf2_ros tf2_echo map odom`、`ros2 run tf2_ros tf2_echo odom base_scan`、
  `ros2 topic echo /scan --once | grep frame_id`、`ros2 topic echo /odom --once | grep -A2 frame_id`。

### 3.14 状态健康检查协议（一切"不动了"先跑这套）
- `ros2 node list`、`ros2 topic list`、`ros2 action list`
- `ros2 topic info /odom`、`hz /odom`、`hz /scan`、`hz /clock`、`hz /map`
  （Publisher count 0 / 无频率 = 仿真或对应节点没跑）
- `ros2 topic echo /odom --once`、`ros2 topic echo /tf --once`
- 手动发速度测动：`ros2 topic pub -1 /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.1}}"`（停：x:0.0）
- **典型假象**：话题全空但以为在跑 → `ps -ef | grep -E "gzserver|slam|nav2"`；
  gzserver 终端被 Ctrl-C 后一切话题消失（/clock、/odom、/scan、/map 全没 Publisher）。

---

## 4. 工程文件清单

| 路径（工作区） | 内容 |
|---|---|
| `D:\harness\vacuum_ws\SETUP.md` | 标准流程文档（与 §2 同源，终端 2 已改用 run_slam.sh） |
| `...\src\vacuum_description\urdf\vacuum.urdf` | 扫地机 URDF（349mm 圆身+差速轮+LDS；**已含惯性块、轮向修正、~/out:=scan**） |
| `...\worlds\house.world` | 两室公寓（9×9m，墙+门洞+多部件家具；**只用 box/cylinder/sphere**） |
| `...\worlds\room.world` | 简易 10×10 房间（旧版） |
| `...\launch\run_sim.sh` | **标准启动脚本**（gzserver 带 ROS 插件 + 可选 GUI + spawn + rsp） |
| `...\launch\run_slam.sh` | **一键建图脚本**（杀旧→等话题→启动 async slam→自检 map→odom TF，日志 /tmp/slam.log） |
| `...\launch\run_nav_test.sh` | **Nav2 冒烟测试**（激活生命周期→查 TF→发导航目标到床 2.5,2.8；lifecycle get 已包 timeout 5） |
| `...\launch\vacuum_sim.xml` | XML launch（**已弃用**，前端有坑） |
| `...\config\nav2_params.yaml` | Nav2 参数（RPP 10Hz、进度检查放宽、46 个 bt 库、map yaml 路径；AMCL 段存在但不用） |
| `...\src\vacuum_demo\` | C++ 节点：scan_monitor、odom_spinner |
| `...\scripts\` | sync_vacuum.sh / sync_to_share.ps1 |
| VM `~/ros_ws/maps/` | house.pgm + house.yaml（已存两室公寓地图，nav2_params 引用） |
| 共享文件夹 | `robot-share\vacuum_ws\`（同步副本）+ `pdftmp\`（PDF 渲染）+ DC-A566B PDF |
| VM | `~/ros_ws/src/`（含 m-explore-ros2 已编译成功，explore_lite 可用） |

---

## 5. 当前进度与下一步

**已完成**：
- ✅ P0 第一环：扫地机在 Gazebo 里能动（走/转/雷达 valid=360/里程计/TF）
- ✅ P0 第二环：手动遥控建出两室公寓地图并保存（`house.pgm/yaml`）
- ✅ explore_lite 源码编译成功（`~/ros_ws/src/m-explore-ros2`）
- ✅ Nav2 全家桶装好；`navigation_launch.py` 能起；生命周期节点手动激活流程打通（run_nav_test.sh）
- ✅ 绕过 AMCL（必崩，见 §3.11），slam_toolbox 兼作定位；导航 goal 已能被接受

**P0 第三环（已知地图导航）——进行中，两个阻塞**：
1. ⏳ **VM CPU 饥饿**：20Hz 控制环丢帧 → Goal ABORTED（"Control loop missed its desired rate" + "Failed to make progress"）。
   已把 RPP/velocity_smoother 降到 10Hz、进度检查放宽（0.1 / 20），**待验证**。
2. ⏳ **map→odom TF 缺失**（诊断中，见 §3.12）：先确认 slam 活着（`run_slam.sh`），必要时静态 TF 旁路分离问题。
   - 验证手段：`bash ~/ros_ws/src/vacuum_description/launch/run_nav_test.sh`
   - 期望：`/navigate_to_pose` (2.5, 2.8) 状态 **SUCCEEDED**（= P0 第三环完成）

**之后**：
- explore_lite 自动探索（已编译，等导航跑通就上）→ P0 第四环：自写 C++ 弓形覆盖清扫（替代 fields2cover 规避 LGPL）+ 回充

**中期路线**：P1 硬件（RK3566+STM32G473 双主控、自定义串口协议）→ P2-P5（见《扫地机器人最终方案.md》五、开发路线图）。

---

## 6. 关键决策速查（避免重蹈覆辙）

1. **launch 用 run_sim.sh 手动脚本，不写 XML launch**（Humble 前端残）
2. **URDF 所有 link 必须带 `<inertial>`**；轮子 `rpy="1.5708 0 0"`（绕 X）
3. **SDF world 只用 box/cylinder/sphere**（cone 不支持）；radius/length 用子元素
4. **gzserver 必须带 `-s libgazebo_ros_init.so -s libgazebo_ros_factory.so`**
5. **雷达重映射 `~/out:=scan`**（新版 gazebo_ros）
6. **teleop 按键 i/j/k/l**（不是 w/s/a/d）；开 teleop 前 `pkill -9 -f odom_spinner`
7. **每次重跑仿真先 `pkill -9 -f gzserver; pkill -9 -f gzclient`**（端口 11345）
8. **虚拟机默认无头跑**（gzclient 吃爆 CPU）；要 3D 先开 VMware 3D 加速并重启 VM
9. **保存地图前先 `hz /map` 确认仿真+SLAM 都在跑**
10. **我改文件→复制到共享文件夹（需 danger-full-access 权限）→ VM 跑 sync_vacuum.sh**
11. **AMCL 永久禁用**：定位只靠 slam_toolbox（map→odom）；只 `navigation_launch.py`，绝不 `bringup_launch.py`
12. **nav2 插件名用斜杠**（`nav2_navfn_planner/NavfnPlanner`、`nav2_behaviors/Spin`）；bt 库列表抄官方 params（46 个，Humble 无 goal_updated_bt_node）
13. **nav2 节点手动 configure+activate**（autostart 不可靠）；nav 相关命令一律包 `timeout 5` 防卡死
14. **slam 用 run_slam.sh 一键重启**；async slam 收不到 /scan 就不发 map→odom，sim 重启后 slam 必须跟着重启
15. **会话间隔后全套重启**：pkill gzserver/gzclient/slam_toolbox/nav2_bringup/component_container → run_sim.sh → run_slam.sh → navigation_launch.py → run_nav_test.sh

---

## 7. 遗留风险/待办

- 共享文件夹挂载偶尔失效（重新 `sudo vmhgfs-fuse .host:/ /mnt/hgfs -o allow_other`）
- modlens 配置需修复（见 §3.10）才能顺畅读用户截图
- explore_lite 已编译，等 P0 第三环（已知地图导航）跑通后接入
- DC-A566B 烧录需要向定昌电子索取 update.img 与 Loader 进入方法
- **MarkToolbox-Setup-v1.0.33（定昌烧录工具）Win10 双击报 "Unknown Hard Error"**：疑似中文路径/缺 VC++ 运行库/.NET；待试：英文路径、管理员运行、VC++ 2015-2022 redist、.NET 3.5、Win7 兼容模式、杀软白名单
- 项目尚未 git 化：用户问过"提交到 main 分支"，等用户拍板是否 `git init`（建议 VM `~/ros_ws/src` 或共享文件夹）
- 定位依赖 slam_toolbox（AMCL 绕过后）：量产前需评估"开机重定位"方案（AMCL 修复 / 地图匹配重定位）
- 虚拟机 3D 加速与内存升级尚未在 VMware 里落实（用户提过要加内存）
